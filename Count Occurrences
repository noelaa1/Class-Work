def linearSearch(arr, targetVal):
    count = 0
    for i in range(len(arr)):
        if arr[i] == targetVal:
            count += 1
    return count

numbers = [1, 3, 7, 3, 5, 3, 9, 3]
print("Count of 3:", linearSearch(numbers, 3))
