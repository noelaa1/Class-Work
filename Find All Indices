def linearSearch(arr, targetVal):
    indices = []
    for i in range(len(arr)):
        if arr[i] == targetVal:
            indices.append(i)
    return indices

data = [4, 2, 4, 8, 4, 1, 4]
print("All indices of 4:", linearSearch(data, 4))
